---

---

<style>
    .cs-footer {
        position: relative;
        background-color: var(--surface-9-dark);
        padding-block: var(--size-fluid-5);
        margin-top: auto; 
        border-top: 0.25rem solid var(--surface-3-dark);

        &:before {
            content: '';
            height: 100%;
            width: 100%;
            position: absolute;
            inset: 0;
            background: linear-gradient(to right, var(--surface-2-dark), var(--brand-primary));
            opacity: 0.25;
        }
    }
</style>
<footer class="cs-footer"></footer>
